#
# This workflow calls the main distribution pipeline from DuckDB to build, test and (optionally) release the extension
#
name: Main Extension Distribution Pipeline
on:
  push:
  pull_request:
  workflow_dispatch:

permissions:
  id-token: write
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}-${{ github.head_ref || '' }}-${{ github.base_ref || '' }}-${{ github.ref != 'refs/heads/main' && github.sha || '' }}
  cancel-in-progress: true

jobs:
  # Build for DuckDB v1.4.4 (previous stable)
  duckdb-v1_4_4-build:
    name: Build extension (DuckDB v1.4.4)
    uses: duckdb/extension-ci-tools/.github/workflows/_extension_distribution.yml@v1.4.4
    with:
      duckdb_version: v1.4.4
      ci_tools_version: v1.4.4
      extension_name: anofox_scenario
      artifact_postfix: _v1.4.4
      exclude_archs: windows_amd64_mingw

  duckdb-v1_4_4-deploy:
    name: Deploy extension (DuckDB v1.4.4)
    needs: duckdb-v1_4_4-build
    uses: ./.github/workflows/_extension_deploy.yml
    secrets: inherit
    with:
      duckdb_version: v1.4.4
      extension_name: anofox_scenario
      artifact_postfix: _v1.4.4
      exclude_archs: windows_amd64_mingw
      deploy_latest: ${{ startsWith(github.ref, 'refs/tags/v') || github.ref == 'refs/heads/main' }}
      deploy_versioned: ${{ startsWith(github.ref, 'refs/tags/v') || github.ref == 'refs/heads/main' }}

  # Build for DuckDB v1.5.3 (latest stable)
  duckdb-v1_5_3-build:
    name: Build extension (DuckDB v1.5.3)
    uses: duckdb/extension-ci-tools/.github/workflows/_extension_distribution.yml@v1.5.3
    with:
      duckdb_version: v1.5.3
      ci_tools_version: v1.5.3
      extension_name: anofox_scenario
      artifact_postfix: _v1.5.3
      exclude_archs: windows_amd64_mingw

  duckdb-v1_5_3-deploy:
    name: Deploy extension (DuckDB v1.5.3)
    needs: duckdb-v1_5_3-build
    uses: ./.github/workflows/_extension_deploy.yml
    secrets: inherit
    with:
      duckdb_version: v1.5.3
      extension_name: anofox_scenario
      artifact_postfix: _v1.5.3
      exclude_archs: windows_amd64_mingw
      deploy_latest: ${{ startsWith(github.ref, 'refs/tags/v') || github.ref == 'refs/heads/main' }}
      deploy_versioned: ${{ startsWith(github.ref, 'refs/tags/v') || github.ref == 'refs/heads/main' }}
